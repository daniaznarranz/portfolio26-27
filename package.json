{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Portafolio con React y Node.js",
  "scripts": {
    "dev": "npm run dev --prefix frontend-react",
    "dev:react": "npm run dev --prefix frontend-react",
    "dev:backend": "npm run dev --prefix backend"
  }
}
